import { useDispatch, useSelector } from 'react-redux';
import { useEffect } from 'react';

export default function FishingSpotPage() {
    const dispatch = useDispatch();

    return (
        <div>
            <h2>Fishing Spot Page</h2>
        </div>
    )
}
